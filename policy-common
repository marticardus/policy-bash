#!/bin/bash
logging(){
	if [ -z "$POLICYNAME" ]; then POLICYNAME=default; fi
	logger -t "policy-bash[${protocol_state}/${POLICYNAME}]" -- $@
}
policyreturn(){
	ACTION=$1; shift
	logging "Return action $ACTION"
	echo "action=$ACTION $@"
	echo "";
}
accept(){ 
	policyreturn "OK" 
}
ret4(){ 
	RET=$1; shift; policyreturn $RET $@ 
}
ret5(){ 
	RET=$1; shift; policyreturn $RET $@ 
}
reject(){ 
	policyreturn REJECT $@ 
}
defer(){ 
	policyreturn DEFER $@ 
}
defer_if_reject(){ 
	policyreturn defer_if_refect $@ 
}
defer_if_permit(){ 
	policyreturn defer_if_permit $@ 
}
discard(){ 
	policyreturn discard $@ 
}
dunno(){ 
	policyreturn DUNNO 
}
hold(){ 
	policyreturn HOLD $@ 
}
warn(){ 
	policyreturn WARN $@ 
}
